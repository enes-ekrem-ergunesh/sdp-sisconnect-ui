import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ns.ionic.sisconnect.ui',
  appName: 'sdp-sisconnect-ui',
  webDir: 'www'
};

export default config;
